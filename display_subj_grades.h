//
// Created by bunyod_khakimov on 25/12/2020.
//
using namespace std;

#ifndef GPA_DISPLAY_SUBJ_GRADES_H
#define GPA_DISPLAY_SUBJ_GRADES_H

#endif //GPA_DISPLAY_SUBJ_GRADES_H

void display_subj_grades(){
    cout<<"\n\n";
    cout<<"==================================\n";
    cout<<"========Calculation Result========\n   ";
    cout<<s1.name<<" = "<<s1.grd<<"\n   ";

    if(s.num<=9 && s.num>=2){
        cout<<s2.name<<" = "<<s2.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=3){
        cout<<s3.name<<" = "<<s3.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=4){
        cout<<s4.name<<" = "<<s4.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=5){
        cout<<s5.name<<" = "<<s5.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=6){
        cout<<s6.name<<" = "<<s6.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=7){
        cout<<s7.name<<" = "<<s7.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=8){
        cout<<s8.name<<" = "<<s8.grd<<"\n   ";
    }

    if(s.num<=9 && s.num>=9){
        cout<<s9.name<<" = "<<s9.grd<<"\n   ";
    }

}