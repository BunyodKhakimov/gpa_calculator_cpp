//
// Created by bunyod_khakimov on 25/12/2020.
//

#ifndef GPA_ENTER_TOTAL_MARK_H
#define GPA_ENTER_TOTAL_MARK_H

#endif //GPA_ENTER_TOTAL_MARK_H

double enter_total_mark(){
    cout<<" \nPlease enter your total mark of 100% for each courses below \n "<<endl;

    cout<<" 1) Enter total mark  of "<<s1.name<<"... ";
    cin>> s1.total_mark;

    if(s.num<=9 && s.num>=2){
        cout<<" 2) Enter total mark  of "<<s2.name<<"... ";
        cin>> s2.total_mark; }

    if(s.num<=9 && s.num>=3){
        cout<<" 3) Enter total mark  of "<<s3.name<<"... ";
        cin>> s3.total_mark; }

    if(s.num<=9 && s.num>=4){
        cout<<" 4) Enter total mark  of "<<s4.name<<"... ";
        cin>> s4.total_mark; }

    if(s.num<=9 && s.num>=5){
        cout<<" 5) Enter total mark  of "<<s5.name<<"... ";
        cin>> s5.total_mark; }

    if(s.num<=9 && s.num>=6){
        cout<<" 6) Enter total mark  of "<<s6.name<<"... ";
        cin>> s6.total_mark; }

    if(s.num<=9 && s.num>=7){
        cout<<" 7) Enter total mark  of "<<s7.name<<"... ";
        cin>> s7.total_mark; }

    if(s.num<=9 && s.num>=8){
        cout<<" 8) Enter total mark  of "<<s8.name<<"... ";
        cin>> s8.total_mark; }

    if(s.num<=9 && s.num==9){
        cout<<" 9) Enter total mark  of "<<s9.name<<"... ";
        cin>> s9.total_mark; }

}